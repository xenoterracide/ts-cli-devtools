{
  "name": "ghb",
  "packageManager": "yarn@4.18.0",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@eslint/json": "^2.0.0",
    "@eslint/markdown": "^8.0.0",
    "@prettier/plugin-xml": "^3.4.2",
    "@vitest/coverage-v8": "^5.0.0",
    "chalk": "^6.0.0",
    "concurrently": "^10.0.0",
    "eslint": "^10.0.0",
    "git-conventional-commits": "^2.8.0",
    "globals": "^17.0.0",
    "jiti": "^2.0.0",
    "lint-staged": "^17.0.0",
    "prettier": "^3.6.2",
    "prettier-plugin-java": "^2.7.5",
    "prettier-plugin-properties": "^0.3.0",
    "prettier-plugin-toml": "^2.0.6",
    "rimraf": "^6.1.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^5.0.0"
  },
  "scripts": {
    "contribute": "{ [ -d .venv ] || uv venv --relocatable; } && uv sync --frozen && git config core.hooksPath .share/git/hooks",
    "merge:copilot": "yarn ghb pr merge --copilot",
    "merge:junie": "yarn ghb pr merge --junie",
    "merge:kimi": "yarn ghb pr merge --kimi",
    "secrets": "yarn ghb secrets",
    "ghb": "yarn workspace @xenoterracide/ghb run ghb",
    "lint:prettier": "prettier --ignore-unknown --cache --check '**'",
    "lint:reuse": "uv run --frozen --group dev reuse lint",
    "lint:eslint": "eslint .",
    "lint": "yarn lint:eslint && yarn lint:prettier && yarn lint:reuse",
    "test": "vitest run --coverage",
    "typecheck": "yarn build && yarn workspaces foreach --all --exclude ghb run typecheck",
    "build": "yarn workspaces foreach --all --exclude ghb --topological run build",
    "check": "concurrently --names eslint,tsc,test --prefix-colors cyan,magenta,green 'yarn lint:eslint' 'yarn typecheck' 'yarn test'"
  },
  "workspaces": [
    "packages/*"
  ]
}
